---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: task-pv-claim
  labels:
    type: aws
    storage: small
spec:
  storageClassName: aws
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi